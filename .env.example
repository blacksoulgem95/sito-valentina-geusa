# ============================================
# Configurazione Firebase
# ============================================
# Le variabili Firebase devono avere il prefisso PUBLIC_ per essere accessibili
# dal client-side. Ottieni questi valori dalla console Firebase:
# https://console.firebase.google.com/

PUBLIC_FIREBASE_API_KEY=your_firebase_api_key_here
PUBLIC_FIREBASE_AUTH_DOMAIN=your_project_id.firebaseapp.com
PUBLIC_FIREBASE_PROJECT_ID=your_project_id
PUBLIC_FIREBASE_STORAGE_BUCKET=your_project_id.appspot.com
PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your_messaging_sender_id
PUBLIC_FIREBASE_APP_ID=your_firebase_app_id

# ============================================
# Configurazione Mailgun (Email)
# ============================================
# Configurazione per l'invio delle email dal form contatti.
# Ottieni questi valori dal dashboard Mailgun:
# https://app.mailgun.com/

MAILGUN_DOMAIN=your_mailgun_domain.com
MAILGUN_API_KEY=your_mailgun_api_key_here
CONTACT_EMAIL=your_email@example.com

# ============================================
# Configurazione Server (Opzionale)
# ============================================
# Queste variabili sono opzionali e hanno valori di default

# Porta del server (default: 8080)
# PORT=8080

# Host del server (default: 0.0.0.0)
# HOST=0.0.0.0

# Ambiente di esecuzione (development/production)
# NODE_ENV=development

# Cloudflare Turnstile (CAPTCHA)
# Ottieni le chiavi da https://dash.cloudflare.com/
# Site Key: pubblica, usata nel frontend
PUBLIC_TURNSTILE_SITE_KEY=your-turnstile-site-key
# Secret Key: privata, usata nel backend per verificare i token
TURNSTILE_SECRET_KEY=your-turnstile-secret-key